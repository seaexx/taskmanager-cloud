apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: taskmanager-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - staging.157.230.65.42.nip.io
    secretName: taskmanager-tls-staging
  rules:
  - host: staging.157.230.65.42.nip.io
    http:
      paths:
      - pathType: Prefix
        path: /api
        backend:
          service:
            name: backend-staging
            port:
              number: 3000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-staging
            port:
              number: 80
